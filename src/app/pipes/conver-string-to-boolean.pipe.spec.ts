import { ConverStringToBooleanPipe } from './conver-string-to-boolean.pipe';

describe('ConverStringToBooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverStringToBooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
